<nav class="navbar navbar-expand-lg navbar-light bg-blue">
  <div class="container">
    <a class="navbar-brand" href="#">Jungslist</a>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <!--<ul class="navbar-nav ml-auto">  
        <li class="nav-item">
          <input class="form-control mr-sm-2" type="text" id="search-input" placeholder="Search...">
        </li>
            <li class="nav-item">
          <button class="btn btn-primary my-2 my-sm-0" id="search-button" type="button">Search</button>
        </li>
    
      </ul>-->
        <form class="form-inline ml-auto" id="search-form">
        <input class="form-control mr-sm-2" type="text" id="search-input" placeholder="Search...">
        <button class="btn btn-primary my-2 my-sm-0" id="search-button" type="button">Search</button>
      </form>
    </div>
  </div>
</nav>

<section class="product-container product-row mt-4">
  <div class="container">
    <div class="row">
      {{#each actualProductData}}
      <div class="col-sm-6 col-md-4 col-lg-3 mb-4">
        <div class="card">
          <div class="card-body">
            <h5 class="card-title">{{product_name}}</h5>
            <p class="card-text">${{price}}</p>
             <p class="card-text">{{seller_location}}</p>
          </div>
        </div>
      </div>
      {{/each}}
    </div>
  </div>
</section>
<script src="./js/searchpage.js"></script>